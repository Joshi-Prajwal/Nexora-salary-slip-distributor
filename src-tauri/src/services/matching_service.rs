use crate::matching::MatchCandidate;

pub struct MatchingService;

impl MatchingService {
    pub fn new() -> Self {
        Self
    }

    pub fn run_matching(&self) -> Vec<MatchCandidate> {
        vec![]
    }
}
